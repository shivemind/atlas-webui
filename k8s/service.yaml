apiVersion: v1
kind: Service
metadata:
  name: atlas-webui
  namespace: atlas-production
  labels:
    app: atlas-webui
spec:
  type: ClusterIP
  selector:
    app: atlas-webui
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
