apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-webui
  namespace: atlas-production
  labels:
    app: atlas-webui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-webui
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: atlas-webui
    spec:
      containers:
        - name: atlas-webui
          image: PLACEHOLDER_ECR_REGISTRY/atlas-webui:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: atlas-webui-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
      terminationGracePeriodSeconds: 30
